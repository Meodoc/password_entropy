# Password Entropy Calculation Scripts

Some very basic scripts for calculating entropy of passwords and passphrases. Useful for some quick checks and tinkering with entropy.

NOTE: **Don't** use this for actual production passwords! As by doing so, the passwords will be exposed in your shell history.  Only do this if you know what you are doing!
